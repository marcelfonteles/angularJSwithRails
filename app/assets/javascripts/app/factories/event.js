'use strict';
var app = angular.module('app02');

/*
app.factory('Event', ['$resource', '$log', function($resource, $log) {
  $log.log($resource('/api/events/:id.json', { id: '@id'}))
  return $resource('/api/events/:id.json', { id: '@id'});
}]);
*/